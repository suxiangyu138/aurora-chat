# 自定义API国产AI大模型聊天安卓APP开发方案（适配iQOO）

## 一、项目整体定位

本项目是一款通用型自定义AI大模型聊天安卓客户端，核心区别于市面上固定模型的AI APP，完全开放自定义配置：用户可手动输入任意AI大模型的接口URL、API Key，支持接入文心一言、通义千问、讯飞星火、百川、智谱清言、DeepSeek等所有主流国产大模型，同时兼容OpenAI、Claude等海外模型接口。

APP针对性适配全系iQOO手机（骁龙旗舰/次旗舰平台、OriginOS系统），解决OriginOS后台杀进程、网络权限限制、适配分辨率、高刷适配等机型专属问题，保证iQOO设备上稳定运行、对话不中断、后台常驻。

核心亮点：全模型通用、用户自主配置、无内置密钥、纯本地配置、轻量化、适配iQOO专属系统。

## 二、核心功能需求（MVP基础版+进阶版）

### 1. 基础核心功能（必做，上线必备）

- 自定义模型配置：支持用户手动填写接口URL、API Key、请求超时时间、模型名称、温度参数、最大生成长度，支持保存多套模型配置、一键切换
- 通用AI对话：文本实时流式对话（打字机效果）、单次问答、上下文连续对话、清空对话、单条消息删除
- 会话管理：新建会话、会话列表、会话重命名、历史会话留存、本地缓存对话记录
- 基础参数调节：支持调整AI温度（随机性）、TopP、最大输出tokens、上下文记忆轮数
- 机型适配优化：专门适配iQOO OriginOS系统，解决后台保活、高刷屏适配、网络波动重连问题

### 2. 进阶拓展功能（可选迭代）

- 预设主流国产大模型模板（无需手动填URL，一键填充，仅密钥自定义）：通义千问、文心一言、讯飞星火、智谱、百川等
- 支持流式/非流式切换、网络失败自动重试、密钥错误校验提示
- 对话主题切换、字体大小调节、深色/浅色模式
- 本地数据加密存储、配置备份与导入导出
- 支持图片输入（多模态，兼容支持图文接口的大模型）

## 三、技术选型（安卓原生，适配iQOO全机型）

采用当前安卓最稳定、性能最优的技术栈，完美兼容iQOO全系骁龙处理器、OriginOS 3/4/5系统，无兼容闪退、卡顿问题。

### 1. 开发语言与框架

- 核心语言：Kotlin（安卓官方主推，轻量化、低功耗，适配移动端）
- UI框架：Jetpack Compose（现代化UI，自适应iQOO高刷屏、曲面屏，适配120Hz/144Hz高刷）
- 架构：MVVM架构（解耦、易维护、后续可迭代拓展功能）

### 2. 网络请求核心（关键：兼容所有AI API）

- 网络框架：OkHttp + Retrofit（支持自定义请求头、自定义Body、超时自定义、流式响应）
- 流式解析：SSE数据流解析（适配所有大模型流式对话接口，实现打字机实时输出）
- 核心优势：不绑定任何固定模型协议，通用JSON请求模板，适配99%的AI大模型HTTP API接口

### 3. 本地存储

- 轻量化配置存储：DataStore（存储用户自定义URL、API Key、模型参数）
- 对话记录存储：Room数据库（结构化存储会话、消息内容，读写高效）
- 安全机制：本地密钥加密存储，明文不落地

### 4. 异步与性能优化

- 协程：Kotlin Coroutines（轻量异步，避免ANR卡顿）
- 生命周期管理：Jetpack Lifecycle（页面销毁自动终止网络请求，杜绝后台流量浪费、崩溃）

## 四、核心技术思路（自定义API通用适配原理）

所有国产AI大模型、海外大模型的API均基于HTTP POST + JSON + SSE流式响应，本APP核心设计一套通用可配置请求模板，实现一套代码适配所有模型。

### 1. 通用请求结构设计（用户自定义全覆盖）

用户输入参数后，APP自动组装标准请求体，核心可配置字段：

- 请求地址：用户自定义URL（接口地址）
- 请求头：自动携带 Authorization（API Key）、Content-Type: application/json
- 请求参数：模型名称、用户提问内容、上下文历史、temperature、max_tokens、top_p

针对国产大模型参数差异化问题：APP提供自定义请求体编辑功能，支持用户微调JSON结构，适配特殊模型接口。

### 2. 流式响应统一解析

文心一言、通义千问、讯飞星火、智谱等所有主流国产模型，流式输出均兼容SSE协议，APP内置统一SSE解析器，自动提取返回文本内容，实现统一的打字机对话效果。非流式接口自动兼容普通JSON返回，双向适配。

### 3. 上下文对话实现

本地自动拼接对话历史，用户每发送一条消息，APP自动将历史问答+新问题组装为上下文参数发送给AI接口，实现连续对话，支持自定义记忆轮数，避免上下文过长导致卡顿。

## 五、iQOO机型专属适配方案（重点）

iQOO手机搭载OriginOS系统，存在后台严格杀进程、网络后台限制、高刷适配异常、省电策略限制等问题，专门做针对性适配优化：

### 1. 后台保活适配（解决对话中断）

- 适配OriginOS后台权限：APP内引导用户开启「后台允许运行」「省电策略无限制」「自启动权限」
- 网络请求生命周期优化：切后台时保留短时请求，禁止系统回收网络线程
- 异常重连机制：网络波动、后台冻结恢复后，自动续接对话、重连接口

### 2. 屏幕与性能适配

- 高刷适配：兼容iQOO 120Hz/144Hz/165Hz刷新率，UI动画流畅不卡顿，锁定最优帧率
- 分辨率适配：适配iQOO全系直面屏、曲面屏，自适应屏幕比例，无UI错位、留白问题
- 骁龙平台优化：针对骁龙8+/8 Gen1/8 Gen2/8 Gen3处理器做线程调度优化，降低CPU占用，温控友好

### 3. 网络适配

- 适配OriginOS网络智能切换，WiFi/5G切换时自动保活请求
- 自定义超时时间，解决部分国产模型接口响应慢导致的请求失败问题

## 六、整体开发流程（分步落地，可直接执行）

### 第一步：项目搭建与基础架构（1-2天）

- 新建Android Studio Kotlin项目，配置Jetpack Compose、MVVM架构
- 引入OkHttp、Retrofit、SSE解析、Room、DataStore依赖
- 适配安卓最低版本（兼容绝大多数iQOO机型，建议最低Android 11）

### 第二步：核心网络通用模块开发（2天）

- 封装通用HTTP请求工具类，支持自定义URL、API Key、请求头、请求体
- 开发SSE流式解析工具，实现实时文本输出
- 封装网络异常拦截：密钥错误、接口404、超时、限流等错误提示

### 第三步：本地存储模块开发（1天）

- 实现模型配置的保存、修改、删除、多套配置切换
- 实现会话、消息的本地数据库存储
- 配置信息加密存储，保障API Key安全

### 第四步：UI界面开发（2-3天）

- 首页：会话列表、新建会话按钮
- 对话页：输入框、发送按钮、流式聊天界面、消息气泡
- 配置页：自定义URL、API Key输入框、参数调节、模型管理

### 第五步：iQOO专属适配优化（1-2天）

- 添加OriginOS权限引导、后台保活适配代码
- 高刷、屏幕适配、性能优化
- iQOO真机实测、卡顿、断连、闪退问题修复

### 第六步：联调测试与打包（1天）

- 对接通义千问、文心一言、讯飞星火、智谱等主流国产模型实测联调
- 修复兼容性问题、优化交互体验
- 打包Release正式安装包，适配安卓全系机型

## 七、安全设计（核心重点）

- 无任何后端服务器：所有配置、对话数据仅保存在用户本地手机，不经过第三方服务器，杜绝密钥泄露
- API Key本地加密存储，禁止明文存储、禁止日志输出密钥信息
- 网络请求仅走用户设备本地网络，无数据上传
- 支持用户一键清空所有配置和会话记录，隐私可控

## 八、兼容的主流国产AI大模型清单（开箱可用）

只需填入对应官方接口URL和个人API Key，即可直接调用：

- 阿里通义千问系列
- 百度文心一言系列
- 科大讯飞星火大模型
- 智谱AI GLM系列
- 百川AI、DeepSeek、豆包开源模型
- 所有开源/私有化部署的HTTP接口AI大模型

## 九、项目优势总结

1. 完全自主可控：用户自定义所有接口参数，无平台限制，想用哪个模型就用哪个
2. 专属iQOO适配：解决OriginOS系统通病，后台稳、不杀进程、对话稳定
3. 通用性极强：一套模板兼容所有AI API，无需针对单个模型开发适配
4. 极致安全：纯本地运行，无云端数据上传，密钥隐私绝对安全
5. 轻量化低功耗：原生安卓开发，无冗余框架，iQOO手机运行流畅无发热

## 十、后续迭代方向

基础版本完成后，可低成本拓展：多模态图文对话、语音输入输出、模型快捷模板、云端备份、深色模式、对话导出、插件功能等。

# 开发环境配置说明

> 所有开发工具均安装在 D 盘,不占用 C 盘空间。本文档由环境配置时自动生成。

## 目录结构

| 目录 | 用途 |
|---|---|
| `D:\JDK21` | JDK 21 LTS(安卓构建推荐版本) |
| `D:\JDK25` | JDK 25 LTS(系统原有,勿动) |
| `D:\AndroidSdk` | Android SDK(platform-tools、android-36、build-tools) |
| `D:\Gradle` | Gradle 缓存目录(GRADLE_USER_HOME) |
| `D:\AndroidData` | 安卓用户数据(模拟器 AVD、adb 密钥等) |

## 环境变量(已设置为用户级)

| 变量 | 值 |
|---|---|
| `ANDROID_HOME` / `ANDROID_SDK_ROOT` | `D:\AndroidSdk` |
| `GRADLE_USER_HOME` | `D:\Gradle` |
| `ANDROID_USER_HOME` | `D:\AndroidData` |
| `PATH` 追加 | `D:\AndroidSdk\platform-tools`(adb)、`D:\AndroidSdk\cmdline-tools\latest\bin`(sdkmanager) |

## 开发工具

- **VSCode** 1.136.2(已装)+ Kotlin 插件(fwcd.kotlin)+ XML 插件
- **JDK 21**:构建时在项目 `gradle.properties` 中通过 `org.gradle.java.home=D:\\JDK21` 指定
- **Gradle**:无需单独安装,项目自带 wrapper,首次构建自动下载到 `D:\Gradle`

## 常用命令

```bash
# 编译 Debug APK
./gradlew assembleDebug

# 直接安装到连接的手机(需开启 USB 调试)
adb install -r app/build/outputs/apk/debug/app-debug.apk

# 查看手机实时日志(过滤崩溃/异常)
adb logcat -s AndroidRuntime:E

# 查看已连接设备
adb devices

# 管理 SDK(如升级平台)
sdkmanager --list
```

## 真机调试准备(iQOO)

1. 手机进入 设置 → 系统管理 → 开发者选项(关于手机连点"软件版本号"7次开启)
2. 打开「USB 调试」,连接电脑后手机上允许调试授权
3. 运行 `adb devices` 确认设备已识别

## 注意事项

- 系统 `JAVA_HOME` 仍指向 `D:\JDK25`(保持你原有配置不变),仅安卓项目内使用 JDK 21
- VSCode 的 Kotlin 补全能力有限,主力靠 Claude Code + 编译真机验证
- 如需 Compose 实时预览,以后可补装 Android Studio(SDK 共用,无需重装)
